public class Solution {
    public bool IsPalindrome(int x) {
        if(x < 0){ return false; };
        int reversed= 0;
        int original =x;
       while (x > 0)
       {
        int remainder= x % 10;
